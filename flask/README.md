# Get started with flask

Open anaconda prompt. cd into folder named flask. Run these two commands:

```
set FLASK_APP=app.py
flask run
```
Once running visit these links:

http://localhost:5000

and 

http://localhost:5000/hello/buddy
